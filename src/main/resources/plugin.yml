name: ${project.name}
main: jp.azisaba.lgw.kdstatus.KDStatusReloaded
version: ${project.version}
author: [siloneco , matsu1213]
description: "KDStatus reloaded."
website: ${project.url}
commands:
  mystatus:
    aliases: ["ms", "status", "st"]
    permission: kdstatusreloaded.command.mystatus
  kdstatus:
    alias: ["kds"]
    permission: kdstatusreloaded.command.kdstatus
permissions:
  mystatus.command.mystatus:
    default: true
  kdstatusreloaded.command.kdstatus:
    default: op